const isProd = process.env.NODE_ENV === 'production'

const devUrl = 'http://mcp.aldbim.com/mcp' // 开发api地址
const proUrl = 'http://mcp.aldbim.com/mcp' // 生产环境api地址
// const devUrl = 'http://192.168.31.141:8080/api'

const FaceUrl = isProd ? proUrl : devUrl

export default {
  baseurl: FaceUrl,
  timeout: 10000
}
