const fs = require('fs')
const fse = require('fs-extra')
const path = require('path')


try {
  fse.copySync(src, dst)
  console.log('success!')
} catch (err) {
  console.error(err)
}

const dirArr = [{
  src: 'public',
  dst: 'public'
}, {
  src: 'dist',
  dst: 'dist'
}, {
  src: 'server.js',
  dst: 'server.js'
}]
for (let item of dirArr) {

  var src = path.join(__dirname, '../public/' + item.src);
  var dst = path.join(__dirname, '../outpot/' + item.det)
  
  console.log(item)
}
